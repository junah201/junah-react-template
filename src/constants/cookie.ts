export const ACCESS_TOEKN = {
  key: "access-token",
  // 1000 milliseconds * 60 seconds * 60 minutes * 24 hours * 8 days = 8 days
  expires: 1000 * 60 * 60 * 24 * 30,
};

export const REFERRAL_CODE = {
  key: "ref",
  // 1000 milliseconds * 60 seconds * 60 minutes * 24 hours * 2 days = 2 days
  expires: 1000 * 60 * 60 * 24 * 2,
};
